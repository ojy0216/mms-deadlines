source 'https://rubygems.org'

# Keep the pinned Jekyll dependency set; Actions builds our local generators.
# Auto-loading github-pages forces safe mode and silently skips those generators.
gem "github-pages", group: :jekyll_plugins, require: false
gem 'html-proofer'

gem "webrick", "~> 1.7"
